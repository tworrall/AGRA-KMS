# This is a sample config file that points to a solr server for each environment
development:
  url: <%= "#{ENV['SOLR_URL']}" %>
test: &TEST
  url: <%= "#{ENV['SOLR_URL']}/test" %>
staging:
  url: <%= "#{ENV['SOLR_URL']}" %>
production:
  url: <%= "#{ENV['SOLR_URL']}" %>
