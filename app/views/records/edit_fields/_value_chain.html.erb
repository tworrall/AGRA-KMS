<%= f.input :value_chain, as: :select,
    collection: ValueChainService.select_all_options,
    include_blank: false,
    item_helper: ValueChainService.method(:include_current_value),
    input_html: { class: 'form-control', multiple: true }
%>
